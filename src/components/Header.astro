---
const navItems = [
  { name: "Angebot", href: "/angebot" },
  { name: "Projekte", href: "/projekte" },
];
---

<header class="bg-white border-b border-gray-200">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-4 flex justify-between items-center">
    <a href="/" class="text-2xl font-display font-semibold text-dark">
      Scalaro Python Solutions
    </a>
    <nav class="hidden md:flex space-x-6">
      {navItems.map(item => (
        <a 
          href={item.href}
          class="text-base font-medium text-gray-700 hover:text-primary transition"
        >
          {item.name}
        </a>
      ))}
    </nav>
  </div>
</header>
